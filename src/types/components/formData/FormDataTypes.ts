export interface PaymentFormValues {
  name: string;
  email: string;
}

export interface ShippingFormValues {
  name: string;
  email: string;
  address: string;
  city: string;
  state: string;
  zip: string;
}
